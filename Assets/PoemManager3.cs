using UnityEngine;
using System.Collections;

public class PoemManager3 : MonoBehaviour {
	
	// Use this for initialization
	void Start () {
		Debug.Log ( "Hello World" ); 
		
		GetComponent<TextMesh>().text = "Be the change you seek.";
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
